# ============================================================================
# Nemesis v2 — Python dependencies
#
# Install core (brain/ + doc generation):
#   pip install -r requirements.txt
#
# Add vector search (optional — enables semantic similarity in context_for()):
#   pip install -r requirements.txt -r requirements-vectors.txt
#   or: ./setup.sh --with-vectors
#
# Minimal (brain/ CLI only, no doc or web):
#   pip install networkx
# ============================================================================

# === Core — required for brain/ package and all skills ===
networkx>=3.0

# === Document generation (scripts/rubick_doc.py, /doc skill) ===
python-docx>=1.0

# === Data validation (pydantic used by api_models.py and several scripts) ===
pydantic>=2.0

# === Legacy web server (scripts/api_server.py, scripts/rubick_ui.py) ===
# Not needed for Claude Code CLI usage — only required if running the Flask/FastAPI
# web UI locally. Safe to comment out on fresh installs if you only use Claude Code.
flask>=3.0
fastapi>=0.115
uvicorn>=0.32
sse-starlette>=2.0
python-multipart>=0.0.9

# === Legacy vector backend (deprecated — replaced by LanceDB) ===
# qdrant-client>=1.12   # kept for reference; not used by brain/ package
